{
  "_status": "PENDING RE-APPROVAL -- NOT AN APPROVAL. The prior Andrew Miller approval of runtime inventory 9f5b05c4281c66963842c20eea646a19a0f534fa95e40e39fe9cc6560345bab8 was invalidated on branch claude/p25-7-validation-pack-solve-fix by the PR #36 review fixes. (1) tools/p25_5/licenses.py no longer infers an SPDX identifier from the version-less 'Apache Software License' trove classifier, nor rewrites the distinct SPDX id 'PSF-2.0' to 'Python-2.0' (PEP 639, appendix-mapping-classifiers): safetensors is now recorded as 'Apache Software License' (its only declared signal is that ambiguous classifier) and typing_extensions as 'PSF-2.0' (its License-Expression), both of which change the generated runtime-license-inventory.json payload hash. (2) The signed statement's source-carry clause was corrected: the pack DOES redistribute the WAFT source under waft-src/. A fresh inventory must be generated by a CI dispatch with the runtime legal-review inputs left empty, then re-signed by Andrew Miller against the emitted SHA256, before packaging. CI fails closed while reviewed!=true or the inventory hash mismatches.",
  "_instructions": "To approve: delete the _status and _instructions keys, set inventory_sha256 to the emitted inventory's SHA256, set reviewed to true, keep reviewer 'Andrew Miller', set reviewed_at to an ISO-8601 UTC timestamp with explicit timezone, and keep the statement below (replacing <hash> with the emitted SHA256).",
  "schema_id": "whitewater-p25-runtime-legal-review-v1",
  "inventory_sha256": "<64-hex SHA256 of the WAFT runtime-license-inventory.json regenerated on this branch; pending CI dispatch>",
  "reviewed": false,
  "reviewer": "Andrew Miller",
  "reviewed_at": "<ISO-8601 UTC timestamp, set when signed>",
  "statement": "I, Andrew Miller, reviewed the generated WAFT-validation runtime license and notice inventory with SHA256 <hash>, covering the conda base (python 3.12, conda-pack, setuptools and the transitive system libraries) and the pip-installed export stack (PyTorch CPU, torchvision, numpy, scipy, onnx, onnxruntime CPU, safetensors, timm and their transitive dependencies), each bound to its own licence and notice evidence. I approve this exact runtime inventory for the WAFT export/validation run. The pinned WAFT source is BSD-3-Clause; the WAFT checkpoint is operator-supplied and its file-level licence remains unknown -- this approval covers the packed conda+pip runtime only, not the operator-supplied checkpoint, and not the WAFT source redistributed inside the package under waft-src/, which is separately covered by legal/source-legal-record.json."
}
