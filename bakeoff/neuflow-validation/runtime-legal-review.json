{
  "_status": "PENDING RE-APPROVAL -- NOT AN APPROVAL. The prior Andrew Miller approval of runtime inventory eb64b5ce46a015e501066fef2c22696cbe1ace37eac226bd0debdd63e49b1adb was invalidated on branch claude/p25-7-validation-pack-solve-fix by the PR #36 review fix: tools/p25_5/licenses.py now records the License-Expression 'PSF-2.0' verbatim instead of rewriting it to the distinct SPDX id 'Python-2.0' (PEP 639), so typing_extensions changes the generated runtime-license-inventory.json payload hash. A fresh inventory must be generated by a CI dispatch with the runtime legal-review inputs left empty, then re-signed by Andrew Miller against the emitted SHA256, before packaging. CI fails closed while reviewed!=true or the inventory hash mismatches.",
  "_instructions": "To approve: delete the _status and _instructions keys, set inventory_sha256 to the emitted inventory's SHA256, set reviewed to true, keep reviewer 'Andrew Miller', set reviewed_at to an ISO-8601 UTC timestamp with explicit timezone, and keep the statement below (replacing <hash> with the emitted SHA256).",
  "schema_id": "whitewater-p25-runtime-legal-review-v1",
  "inventory_sha256": "<64-hex SHA256 of the NeuFlow runtime-license-inventory.json regenerated on this branch; pending CI dispatch>",
  "reviewed": false,
  "reviewer": "Andrew Miller",
  "reviewed_at": "<ISO-8601 UTC timestamp, set when signed>",
  "statement": "I, Andrew Miller, reviewed the generated NeuFlow-validation runtime license and notice inventory with SHA256 <hash>, covering the conda base (python 3.9, conda-pack, setuptools and the transitive system libraries) and the pip-installed export stack (PyTorch CPU, torchvision, numpy, onnx, onnxruntime CPU and their transitive dependencies), each bound to its own licence and notice evidence. I approve this exact runtime inventory for the NeuFlow v2 export/validation run. The pinned NeuFlow v2 source is Apache-2.0; the NeuFlow v2 checkpoint is operator-supplied and its file-level licence remains unknown -- this approval covers the packed runtime only, not the checkpoint, and no upstream source or checkpoint is carried in the package."
}
