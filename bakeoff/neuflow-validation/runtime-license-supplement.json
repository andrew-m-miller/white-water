{
  "_note": "Starts EMPTY on purpose. licenses.py fails closed and names any package that lacks embedded info/licenses evidence (or whose licence it cannot derive). The conda spec is now a MINIMAL BASE (python 3.9 + conda-pack + setuptools) and the WHOLE ML export stack is pip-installed at CI build (torch==2.0.1/torchvision==0.15.2 from https://download.pytorch.org/whl/cpu; numpy==1.24.4 onnx==1.14.1 onnxruntime==1.16.3 from PyPI), so this inventory covers those as pip identities (pip://name==version) rather than conda builds. That makes the runtime licence surface SUBSTANTIALLY LARGER than a conda-only solve: the pip torch wheel bundles vendored libraries (its own CPU BLAS/MKL/fbgemm and other embedded third-party code) whose licences must be enumerated in Andrew's review. The first neuflow-validation EL8 dispatch reports the exact packages+URLs to add here -- the conda base packages bound to their CI-solved build, and each pip package (torch and its bundled libs, torchvision, numpy, onnx, onnxruntime) by its wheel -- see bakeoff/p25-6/runtime-license-supplement.json for the shape of a populated file. Do not guess entries before the solve.",
  "schema_id": "whitewater-p25-runtime-license-supplement-v1",
  "packages": []
}
