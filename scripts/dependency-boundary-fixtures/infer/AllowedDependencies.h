// This fixture stands in for the future src/infer until that production layer exists.
// ONNX Runtime and file access are deliberately valid dependencies of inference.
#include <filesystem>
#include <fstream>
#include <onnxruntime_c_api.h>
